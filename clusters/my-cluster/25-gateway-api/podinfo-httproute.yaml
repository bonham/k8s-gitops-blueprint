apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: podinfo
  namespace: podinfo2
spec:
  parentRefs:
    - name: kong-gw
      namespace: kong-system
  hostnames:
    - "podinfo.local"
  rules:
    - backendRefs:
        - name: my-podinfo
          port: 9898
